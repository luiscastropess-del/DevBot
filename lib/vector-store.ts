export async function recall(prompt: string, limit: number = 2): Promise<string[]> {
  return [];
}
export async function remember(category: string, text: string): Promise<void> {}
export async function getEmbedding(text: string): Promise<number[]> {
  return [];
}
